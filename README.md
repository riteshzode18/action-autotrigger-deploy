# action-autotrigger-deploy

## workflow_dispatch

Allows you to manually trigger a workflow from the GitHub Actions UI or via the API.

## workflow_run

Triggers a workflow after another workflow has finished running.
